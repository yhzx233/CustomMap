{
    "name": "${pluginName}",
    "entry": "${pluginFile}",
    "version": "${pluginVersion}",
    "type": "native",
    "dependencies": [
        {
            "name": "LegacyRemoteCall"
        }
    ]
}
